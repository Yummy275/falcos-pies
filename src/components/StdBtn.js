import styled from 'styled-components';

const StdBtn = styled.button`
    cursor: pointer;
    width: 6rem;
    height: 2rem;
    font-size: 0.75rem;
    font-weight: bold;
    margin-top: 1rem;
    border: none;
`;

export default StdBtn;
